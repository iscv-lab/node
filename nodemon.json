{
  "verbose": true,
  "watch": ["./out", "./schema"],
  "ext": "ts,json,js",
  "ignore": [".git", ".gitignore", "access.log", "debug.log", "./public/**/*.spec.ts"],
  "exec": "node --inspect ./out/app.js",
  "env": {
    "NODE_ENV": "development"
  }
}
